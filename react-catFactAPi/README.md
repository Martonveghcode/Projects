# React Cat Fact API

Simple fetch demo that calls `https://catfact.ninja/fact` on load and renders the fact once it returns. Used to practice `useEffect`, basic loading states, and passing data down to a child component.
