# React Forms

Signup form demo that captures email, password, multiple checkbox selections, and a favorite color dropdown. Submits to a handler that logs the collected data object so I could practice working with `FormData` in React.
