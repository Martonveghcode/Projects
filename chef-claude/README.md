# Chef-Claude

Ingredient-driven recipe generator built with React. Add items to your pantry list; once you have a handful, the app calls Google Gemini (`VITE_GEMINI_API_KEY`) to draft a recipe in markdown and scrolls you to the result.
