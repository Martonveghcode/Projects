# Java Learning 1

Small collection of early JavaScript practice files. Includes a template-literal console example plus a simple leads tracker (`index1.html`) that saves URLs from an input into a list of clickable links.
