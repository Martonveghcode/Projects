# React Static Page

Lightweight React page with a navbar and a "Fun facts about React" list. It is a pure static render (no data fetching) built to practice splitting a simple layout into components.
