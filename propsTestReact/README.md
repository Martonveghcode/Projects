# Props Test React

Tiny props drill: the root passes an array of numbers (1–100) into the `App` component, which maps them into paragraphs. Kept minimal to focus on data flow and rendering lists from props.
