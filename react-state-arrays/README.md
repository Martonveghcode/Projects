# React State Arrays

Button-driven demo for growing an array in state: each click appends the next value from a preset list and renders it. Built to practice immutable updates and rendering lists in React.
